[project]
name = "lenlab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pyside6>=6.8.0.1",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.3.3",
]
